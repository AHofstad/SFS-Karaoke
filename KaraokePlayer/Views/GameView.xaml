<UserControl x:Class="KaraokePlayer.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:loc="clr-namespace:KaraokePlayer.Localization"
             xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             xmlns:converters="clr-namespace:KaraokePlayer.Converters"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibility" />
    <sys:Double x:Key="GameBaseWidth">1280</sys:Double>
    <sys:Double x:Key="GameBaseHeight">720</sys:Double>
    <sys:Double x:Key="GameOverlayWidth">240</sys:Double>
    <sys:Double x:Key="GameOverlayHeight">64</sys:Double>
  </UserControl.Resources>

  <Viewbox Stretch="UniformToFill" StretchDirection="Both">
    <Grid Width="{StaticResource GameBaseWidth}" Height="{StaticResource GameBaseHeight}">
      <Border Background="#000000" CornerRadius="0" Margin="0">
        <Grid>
          <Image Source="{Binding BackgroundPath}"
                 Stretch="UniformToFill"
                 Visibility="{Binding ElementName=GameSurface, Path=IsVisible, Converter={StaticResource InverseBoolToVisibility}}" />
          <vlc:VideoView x:Name="GameSurface" />
          <!-- Lyrics rendered in a separate window to avoid airspace issues with VideoView -->
        </Grid>
      </Border>

    </Grid>
  </Viewbox>
</UserControl>
